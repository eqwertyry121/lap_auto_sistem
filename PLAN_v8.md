# ТЗ v8: Честные алерты, заслон перекупам, точные дефекты

> 2026-08-06. Реакция на живые алерты дня: 24 алерта 🦌 ЛОСЬ за сутки, из них
> часть — по лотам, где характеристики указаны в самом объявлении, и по
> перекупским описаниям (примеры: Legion 5 7735HS/4060 €1100 —
> `194373542`, Acer VX15 i5-7300HQ/1050Ti €220 — `194371772`).
> Статус: ДИАГНОСТИКА ЗАВЕРШЕНА, половина объёма ЛОСЬ уже закрыта починкой
> is_trader (см. ниже). Это ТЗ — на остаток.

## 0. Диагностика: что случилось на самом деле

### 0.1 Почему ЛОСЬ сыпался пачками (уже почти закрыто)

Следы обоих лотов в `data/funnel_trace.log` показывают одинаковый сценарий:

```
L3.1 regex: cpu=… ram=… ssd=… gpu=…        ← всё распознано ИЗ ТЕКСТА лота
L4: рыночной группы для конфигурации нет   ← затык здесь
L5: вердикт ЛОСЬ
```

Затык: вердикт ЛОСЬ ставится при `devOK=false` (нет рыночной группы). Группы
K0–K2 в dGPU-режиме требуют ≥5 лотов с ТЕМ ЖЕ CPU + ТЕМ ЖЕ GPU — на пуле из
~723 лотов таких комбинаций почти нет. Фолбэк K3 (гедоника) был `Usable=false`
из-за проблемы is_trader (обучающая выборка пуста). Итог: ЛОСЬ на каждый
лот с распознанным железом — 24 алерта за день.

**Что уже починено (2026-08-06, вечер):** is_trader теперь честный, обучающая
выборка ожила (N=315, R²=0.789, Usable=true — и в dGPU-режиме тоже,
`hedonicTraining` не зависит от dGPU-фильтра). Проверено офлайн-симулятором
`tools/marketsim` (без KP):

| Лот | Было (до починки) | Стало (K3) |
|---|---|---|
| Legion 5 7735HS/4060, €1100 | ЛОСЬ-алерт | K3: рынок €1028, отклонение **+7% → EXPENSIVE, тихо** |
| Acer VX15 7300HQ/1050Ti, €220 | ЛОСЬ-алерт | K3: рынок €367, отклонение **−40% → DIAMOND_SUSPECT, честный алерт** |

После перезапуска 19:28 и до конца дня — **ни одного алерта** (бот в
челлендж-паузах, но механизм проверен симуляцией). ЛОСЬ остаётся только для
железа, которого НЕТ в эталоне hw.db (новые SKU 2026 — балла нет, K3
невозможен) — это редкие случаи.

### 0.2 Почему текст ЛОСЬ врёт (проблема остаётся)

`mooseAlertText` пишет «🦌 ВЛАДЕЛЕЦ ЛОСЬ, НО Я ДОБЫЛ ИНФУ» — формулировка из
политики v7 предполагала кейс «владелец не указал железо, бот добыл сам».
В реальности ЛОСЬ означает только «в данных KP нет группы для сравнения», а
железо в обоих примерах было распознано regex'ом ИЗ САМОГО ОБЪЯВЛЕНИЯ
(источник в трейсе: `regex`). Сообщение клевещет на продавца.

### 0.3 Почему перекуп прошёл L1 (проблема остаётся)

Решением 2026-08-05 L1 детектит магазины ТОЛЬКО по тексту описания (метки KP
тогда врали на 100% — см. починку is_trader). Описание `194371772` не
содержит ни одного маркера словаря v2, хотя перекупские сигналы налицо:

- доставка/самовывоз в **5 городов** (BEOGRAD, ZRENJANIN, KIKINDA, NOVI SAD,
  SUBOTICA) — частник так не делает;
- маркетинговый суперлатив «NAJPOVOLJNIJA CENA LAPTOPA ZA OVAKVU
  KONFIGURACIJU»;
- шаблонный блок TECH SPECS + вариативный прайс («sa punjacem / bez punjaca
  cena je 200e»).

### 0.4 Почему «нюанс: bez punjaca» — ложный (проблема остаётся)

L2 ищет дефекты подстрокой. В описании: «prodaje se **sa punjacem** acer,
ukoliko zelite **bez punjaca** cena je 200e» — зарядка В КОМПЛЕКТЕ, «без
зарядки» — условный вариант со скидкой. Подстроковый матч игнорирует контекст
и отрицание.

---

## 1. Изменение ЛОСЬ: честный текст + порог цены

**Файлы:** `funnel/funnel.go`, `config/config.go`, `.env.example`.

1.1. `mooseAlertText` переписать БЕЗ клеветы на владельца:

```
🦌 РЕДКОЕ ЖЕЛЕЗО — СРАВНИТЬ НЕ С ЧЕМ

<заголовок>
Железо: <строка конфигурации>
Мощность: N баллов · M баллов/€1000      (если баллы есть)
Цена: €X
ℹ️ В наших данных KP нет похожих лотов, а эталон не знает это железо —
оценить цену не с чем. Реши по цифрам выше.
```

Код `ЛОСЬ` и счётчики не менять (аудит/статистика сохраняются).

1.2. Новый гейт `mooseAlertWorthy(priceEUR, cfg.MooseMinEUR)` — аналог
`manualAlertWorthy`: алерт ЛОСЬ только при цене ≥ `MOOSE_MIN_EUR`
(env, дефолт **400** — как у MANUAL; дешевле — тихий исход, код ЛОСЬ
остаётся в аудите). Дешёвое неопознанное железо = шум.

1.3. Тесты: `TestMooseAlertText_NoOwnerClaims` (в тексте нет слов
«владелец/лось/добыл»), `TestMooseAlertWorthy` (границы порога).

## 2. Заслон перекупам: маркеры v3 + честные метки KP

**Файлы:** `filters/filters.go`, `filters/markers.go`, тесты.

2.1. **Новый маркер М7 «мультигород»** (weight 1.0): в описании ≥4 РАЗНЫХ
города из словаря крупных городов Сербии (`cityMarkers`: beograd, novi sad,
nis, kragujevac, subotica, zrenjanin, kikinda, pancevo, cacak, kraljevo,
leskovac, sombor, valjevo, smederevo, sabac, uzice, vranje, jagodina,
bor, ruma, sremska mitrovica, indjija, vrsac, backa palanka, zajecar, pirot).
Частник указывает 0–1 город; список самовывозов ≥4 — поведение перекупа.

2.2. **Новый маркер М8 «суперлативы»** (weight 1.0): `najpovoljnija cena`,
`najpovoljnija ponuda`, `najbolja ponuda`, `najniza cena`. Осторожно: вес 1.0
в одиночке не даёт SHOP (порог 2.0), но в паре с М7/М5 — даёт. Кейс VX15:
М7(1.0) + М8(1.0) = 2.0 → SHOP.

2.3. `MarkersVersion = 3`. Регрессионный тест на РЕАЛЬНОМ описании `194371772`
(взять из funnel_trace/объявления, сохранить в тест как кейс «перекуп с
доставкой по 5 городам») + негативный кейс частного описания с одним городом.

2.4. **ВОПРОС К ПОЛЬЗОВАТЕЛЮ (М1/М2):** решение 2026-08-05 «не использовать
метки KP» принималось, когда флаг Trgovac врал на 100%. Теперь is_trader
честный (title="Trgovac"). Предлагаю вернуть в L1:
- М1: `IsTrader` (заявленный KP торговец) → SHOP;
- М2: `KPIzlog` (витрина) → SHOP.
Это мгновенно выкосит декларированные магазины из алертов (880+ лотов в
датасете, и все будущие). Рекомендуемое значение по умолчанию: **включить** —
продукт охотится за сделками частников. Реализация: 6 строк в начале `L1()`
+ тесты. Без подтверждения пользователя НЕ включать.

2.5. **Обязательная калибровка (процесс проекта, Фаза 2):** после правки
словарей — `cmd/analyze -markers` (когорты впервые реальные: 880 магазинов
kp_izlog vs 2269 частников с описаниями) → `cmd/label -export-top 30` +
`-export-random 30` → ручная разметка → `cmd/filtertest` (гейты:
recall(SHOP) ≥ 0.95, precision(SHOP) ≥ 0.90, FP на PRIVATE ≤ 0.05). Гейты
не прошли — словари дорабатываются, версия не фиксируется.

## 3. Точные дефекты: контекст для «bez punjaca»

**Файлы:** `filters/filters.go`, тесты.

3.1. В `L2` для маркера «bez punjaca» добавить контекстный страж:
- если в тексте есть «sa punjacem» / «punjac ide uz» / «punjac u kompletu» —
  маркер НЕ срабатывает (зарядка в комплекте, «без зарядки» — опция);
- иначе — если в 30 символах ПЕРЕД вхождением есть условное «ukoliko» или
  «ako» — маркер НЕ срабатывает (условное предложение, не дефект).
Остальные дефектные маркеры не трогать (подстрока работает).

3.2. Тесты: реальное описание VX15 (сахар с зарядкой → CLEAN по этому
маркеру), «prodajem bez punjaca» без контекста → DEFECT, «ukoliko zelite
bez punjaca» → не дефект.

## 4. Наблюдаемость

**Файл:** `funnel/funnel.go` (`RefreshMarket`).

4.1. В лог обновления рынка добавить `hedonic_usable`, `hedonic_n`,
`hedonic_r2` — чтобы в bot.log было видно, жив ли K3 (именно его смерть
превращала воронку в ЛОСЬ-фабрику; в следующий раз заметим сразу).

## 5. Порядок работ и приёмка

1. Код пп. 1–4 + тесты; `go build && go vet && go test ./...` зелёные.
2. Прогон `cmd/filtertest` на текущей золотой выборке (не завалить гейты).
3. Офлайн-проверка симулятором: `tools/marketsim` по обоим кейсам дня
   (Legion → EXPENSIVE тихо; VX15 после маркеров v3 — SHOP до L4).
4. Пересборка kpbot.exe/research.exe, перезапуск через watchdog
   (процедура в HANDOFF).
5. Наблюдение 24ч: алерты только DIAMOND / DIAMOND_SUSPECT / CHECK /
   MANUAL(≥400€) / ЛОСЬ(≥400€, редко); в funnel_trace.log ЛОСЬ — только для
   железа вне эталона.
6. Обновить HANDOFF.md (политика v8) и память проекта.

**Открытые решения (за пользователем):**
- (2.4) включать ли М1/М2 (честные метки KP) в L1 — рекомендую ДА;
- (1.2) порог ЛОСЬ-алерта MOOSE_MIN_EUR — рекомендую 400 (как MANUAL),
  варианты: 200 / без порога.
