# ТЗ v5: Из беты в рабочую версию — «Арбитраж ноутбуков с дискретной графикой»

> Статус: ПЛАН (ждёт утверждения пользователем). Дата: 2026-08-05.
> Выше по цепочке: PLAN.md (исходное ТЗ), PLAN_v3.md (воронка), PLAN_v4.md (детерминированная воронка L0–L5 — выполнена).
> Этот документ: новая продуктовая логика (только ноутбуки с дискретной видеокартой) + пошаговый вывод из беты.

---

## 0. Цель и критерий готовности

**Цель:** превратить рутину перепродажи (6 часов/день) в 6 минут/день. Бот делает ВСЁ
до момента «договориться с продавцом»: мониторинг → фильтрация → распознавание железа →
сравнение цены с рынком → сравнение цена/качество по бенчмаркам → готовый кандидат в Telegram.
Пользователь только смотрит финальные алерты и договаривается.

**Критерий «рабочей версии»:** каждый алерт — это ноутбук, про который бот проверил:
1. есть **дискретная видеокарта** (без неё не берём — жёсткое требование);
2. определены **и CPU, и GPU** (точные модели с бенчмарк-баллами);
3. это **не MacBook** (бан по названию, мгновенно);
4. цена **рыночная или ниже**;
5. за эти же деньги (±10%) **нет варианта с заметно мощнее CPU ИЛИ GPU** — то есть
   кандидат лучший по цена/качество в своём бюджете.

Если хотя бы один пункт не выполнен — лот НЕ отправляется как готовый кандидат.

---

## 1. Твоя рутина → что делает бот (соответствие)

| # | Делаешь вручную сейчас | Делает бот |
|---|---|---|
| 1 | Мониторю новые объявления | Поллер: каждые 45 с забирает 30 свежих лотов |
| 2 | Фильтры: без магазинов и шлака | L1 (магазины по тексту) + L2 (хлам/дефекты) + **НОВОЕ: бан MacBook** |
| 3 | Ищу CPU и GPU — без них не беру | L3: regex → Gemini-текст → Gemini-фото → интернет «модель→железо». **НОВОЕ: оба обязательны + обязательна дискретная GPU** |
| 4 | Сравниваю цену с рынком | L4: медиана по dGPU-пулу (research.db) или интернет-ресерч (Gemini+поиск) |
| 5 | Смотрю бенчмарки, сравниваю с другими | **НОВОЕ:** бенчмарк-база hw.db (4239 CPU / 874 GPU, chaynikam.info) вместо «сайта с бенчмарками» |
| 6 | Есть ли мощнее CPU/GPU за эти деньги ±5–10%? | **НОВОЕ:** L5-гейты «мощнее CPU за эти деньги» и «мощнее GPU за эти деньги» |
| 7 | Если нет — вариант хороший, договариваюсь | Алерт с полным досье. Договариваешься ты (в MVP авто-переписка вне скоупа) |

---

## 2. Ответ на ключевой вопрос: кто и когда ходит в Gemini?

Порядок строго такой (детерминизм первичен, Gemini — только там, где regex/математика бессильны):

```
фильтры (бот, 0 запросов)
  → железо: regex (бот) → Gemini-текст → Gemini-фото → интернет «модель→железо» (Gemini+поиск)
  → цена: медианы research.db (бот) → если медианы нет: интернет-ресерч цены (Gemini+поиск)
  → бенчмарки: hw.db (бот, чисто детерминированно — Gemini НЕ участвует)
  → вердикт (бот) → алерт
```

То есть: **рынок и бенчмарки считает сам бот** (медианы + баллы hw.db), а Gemini используется
только для (а) извлечения железа из текста/фото, когда regex не справился, и (б) интернет-ресерча
(цена или «модель→железо»), когда локальных данных нет. Gemini не принимает решений о цене/выгоде —
он только добывает факты, вердикт всегда детерминированный.

---

## 3. ЛОГИКА ДОСКОНАЛЬНО (самое важное)

### 3.0 Определения

- **dGPU (дискретная видеокарта)** — отдельный графический чип: NVIDIA (GeForce GTX/RTX, MX,
  Quadro, T-series, RTX A-series), AMD (Radeon RX, Radeon Pro), Intel Arc. Встроенная
  (Intel HD/UHD/Iris, «Radeon Graphics» без номера) — НЕ считается.
- **Бенчмарк-балл** — числовая мощность из hw.db (CPU: hw_cpu, GPU: hw_gpu). Чем выше — тем мощнее.
- **«За те же деньги»** — ценовой коридор ±PRICE_BAND_PCT% (по умолчанию ±10%) от цены кандидата.
- **«Заметно мощнее»** — бенчмарк-балл ≥ балл кандидата × (1 + STRONGER_PCT/100)
  (по умолчанию +10%; пользовательский диапазон 5–10%, ниже — шум).

### 3.1 Конвейер (порядок строго фиксирован: дёшево → дорого)

```
новые 30 лотов (search, каждые 45 с)
 │
 L0  бан-лист + sanity ЦЕНЫ/обмена        детерминирован, 0 запросов, ДО /eds/
 │   - MacBook в названии → BAN_MAC (мгновенный тихий скип, даже без /eds/)
 │   - цена → EUR; 1–5000€; exchange/zamena → SANITY (тихо)
 │
 ├── /eds/{id} — детали (только прошедшие L0)
 │
 L1  магазины (по тексту описания)        детерминирован → SHOP (тихо)
 L2  хлам/дефекты                         детерминирован → JUNK (тихо) / DEFECT→CHECK
 │
 L3  железо: CPU и GPU (ОБА нужны)
 │   L3.1 regex по заголовку+описанию
 │   L3.2 Gemini-текст (если regex не дал CPU или GPU)
 │   L3.3 Gemini-фото (если всё ещё нет)
 │   L3.4 интернет «модель→железо»: если нашли модель ноутбука, но не нашли
 │        CPU/GPU — ищем в интернете спеки этой модели
 │   → есть модель, но CPU или GPU так и не определены → MANUAL (алерт от 400€)
 │   → GPU отсутствует или только встроенная → NO_GPU (ТИХО, не шлём)
 │
 L4  цена: нормализация EUR → медиана по dGPU-пулу (K0→K1→K2, GPU-измерение
 │    сохраняется на всех уровнях) → если медианы нет: интернет-ресерч цены
 │   → цена выше рынка более чем на MARKET_TOL_PCT → EXPENSIVE (тихо)
 │
 L5  цена/качество (бенчмарки CPU и GPU)
 │   в коридоре ±PRICE_BAND_PCT% ищем лоты с дискретной графикой:
 │   - есть CPU мощнее на ≥STRONGER_PCT%?  → не лучший вариант
 │   - есть GPU мощнее на ≥STRONGER_PCT%?  → не лучший вариант
 │   - ничего мощнее нет → кандидат лучший по цена/качество
 │
 L6  вердикт + алерт-досье
```

### 3.2 L0 — бан-лист и sanity (ДО запроса /eds/, экономим лимиты KP)

Вход: заголовок + цена + валюта + exchange + snippet (всё из search-ответа, 0 запросов).
1. **MacBook-бан:** нормализованный заголовок содержит `macbook` (а также `apple macbook`) →
   вердикт **BAN_MAC**, тихий скип, лот помечается в БД `SKIPPED_BAN` (чтобы не обрабатывать повторно).
   Это происходит в `main.go pollOnce` сразу после дедупликации, **до** `/eds/`.
2. Цена → EUR (курс живой из kv-кэша). Диапазон €1–5000, иначе SANITY (тихо).
3. `exchange=true` или «zamena» в тексте → SANITY (тихо).
Выход: прошёл → заказываем `/eds/{id}`.

### 3.3 L1 / L2 — магазины и хлам (без изменений)
Как в PLAN_v4: L1 режет только по тексту описания (метки KP не используются),
L2 — маркеры PARTS_ONLY/DEFECT + кросс-чек ценой. Ничего не ломаем.

### 3.4 L3 — железо: CPU и GPU оба обязательны + обязательна дискретная GPU

Ступени (вызываются только пока не хватает CPU **или** GPU):
- **L3.1 regex** — извлекает CPU/RAM/SSD/GPU из текста (бесплатно).
- **L3.2 Gemini-текст** — если regex не дал CPU или GPU.
- **L3.3 Gemini-фото** — отправляет до 5 фото, просит максимум: модель ноутбука,
  CPU, GPU (явно допустим ответ «integrated»), RAM/SSD, и `why_no_cpu` если CPU не найден.
- **L3.4 интернет «модель→железо» (НОВОЕ)** — если после L3.3 известна МОДЕЛЬ ноутбука
  (например «Lenovo Legion 5 15ACH6»), но всё ещё нет CPU или GPU: Gemini+Google Search
  ищет в интернете официальные спеки этой модели и возвращает CPU/GPU. Найденные значения
  валидируются по hw.db (есть балл → принимаем).

Исходы после всех ступеней:
- **GPU пустой или «integrated» → NO_GPU (тихо).** Ноутбуки без дискретной видеокарты
  пользователю не интересны — не шлём вообще (ни алертом, ни «посмотреть»). Счётчик для статистики.
- **CPU не определён** (а GPU мог быть) → MANUAL: алерт «👀 НУЖНО ПОСМОТРЕТЬ» только при
  цене ≥ MANUAL_MIN_EUR (400€), ниже — тихий аудит.
- **Оба определены** → идём в L4.

Примечание: если на ступени L3.4 интернет назвал CPU/GPU, но в hw.db нет балла для него —
значит мы не можем честно сравнить бенчмарк → трактуем как «не определено» (MANUAL/NO_GPU по ситуации),
не выдумываем мощность.

### 3.5 L4 — цена: рынок для dGPU-пула

1. Цена нормализуется в EUR (единый владелец — pricing.ToEUR).
2. **Медиана считается только по пулу ноутбуков с дискретной графикой** (dGPU-пул):
   магазины/хлам/BROKEN/старые исключены, как и раньше, плюс обязательный фильтр `gpu_score>0`.
3. Иерархия групп **сохраняет GPU-измерение на каждом уровне**, чтобы игровой ноут не
   сравнивался с офисным:
   - K0 = cpu | ram_band | ssd_band | gpu_model (самая точная, min n=8 если наберётся)
   - K1 = cpu | ram_band | gpu_model (min n=5)
   - K2 = cpu | gpu_model (min n=5)
   (Раньше K1/K2 отбрасывали GPU — из-за этого офисный Ultra 5 сравнился с игровыми i9.)
4. Если ни одна группа не набрала n → **интернет-ресерч цены** (уже реализован: Gemini+Google
   Search, уровень «WEB»). Не дал цены → NO_MARKET (тихо).
5. Отклонение `dev = цена/рынок − 1`. Если `dev > +MARKET_TOL_PCT%` (по умолчанию +5%) →
   **EXPENSIVE (тихо)** — дороже рынка, не интересно. Иначе (рыночная или ниже) → в L5.

### 3.6 L5 — цена/качество: мощнее ли CPU или GPU за эти деньги?

Вопрос пользователя: «есть ли на рынке CPU мощнее за эти деньги ±5–10% или GPU мощнее за эти
деньги ±5–10%? Если нет — вариант хороший».

Формализация:
- Коридор цены кандидата: `[цена×(1−B), цена×(1+B)]`, B = PRICE_BAND_PCT% (10%).
- Кандидаты для сравнения — лоты dGPU-пула (свежие, не магазины, не хлам, cpu_score>0, gpu_score>0),
  чья цена внутри коридора.
- **CPU-гейт:** существует ли кандидат с `cpu_score ≥ cpu_score_кандидата × (1+S)` (S = STRONGER_PCT%, 10%)?
- **GPU-гейт:** существует ли кандидат с `gpu_score ≥ gpu_score_кандидата × (1+S)`?
- Если **CPU-гейт ИЛИ GPU-гейт сработал** → кандидат НЕ лучший по цена/качество:
  вердикт **CHECK** («есть мощнее за эти деньги») с перечислением этих альтернатив —
  пользователь сам решает, но как «готовый кандидат» лот не проходит.
- Если **оба гейта пусты** → кандидат — лучший по цена/качество в своём бюджете → переходим к финальному вердикту.

### 3.7 L6 — финальный вердикт (для лучших по цена/качество)

Для лотов, прошедших L5 (ничего мощнее за эти деньги нет):
- `dev ≤ SUSPECT_DEV_PCT` (−40%) → **SUSPECT** 💎❓ (подозрительно дёшево, возможна приманка) — алерт с предупреждением;
- `dev ≤ DIAMOND_DEV_PCT` (−15%) → **DIAMOND** 💎 (выраженный алмаз) — алерт;
- иначе → **GOOD** ✅ (рыночная/чуть ниже, лучший по цена/качество) — алерт.

Все три исхода — алерты-досье (раздел 4). Тихие исходы: SANITY, BAN_MAC, SHOP, JUNK,
NO_GPU, EXPENSIVE, NO_MARKET — только аудит в БД и счётчики.

### 3.8 Сводная таблица вердиктов

| Код | Условие | Telegram |
|---|---|---|
| BAN_MAC | MacBook в названии | тихо |
| SANITY | цена вне €1–5000 / обмен | тихо |
| SHOP | магазин по тексту | тихо |
| JUNK | труп/запчасти | тихо |
| NO_GPU | нет дискретной видеокарты | тихо |
| MANUAL | CPU не определён | 👀 алерт от 400€ |
| EXPENSIVE | дороже рынка | тихо |
| NO_MARKET | рыночной оценки нет (и интернет не дал) | тихо |
| CHECK | дефект ИЛИ есть мощнее CPU/GPU за эти деньги | ⚠️ алерт с альтернативами |
| SUSPECT | лучший по цена/качеству, но dev < −40% | 💎❓ алерт с предупреждением |
| DIAMOND | лучший по цена/качеству, dev ≤ −15% | 💎 алерт |
| GOOD | лучший по цена/качеству, цена ≤ рынка | ✅ алерт |

---

## 4. Формат алерта-досье (L6)

```
💎 АЛМАЗ   (или ✅ ХОРОШИЙ ВАРИАНТ / 💎❓ ПОДОЗРИТЕЛЬНО ДЁШЕВО)

HP Victus 15 (RTX 3050)
Железо: Ryzen 5 5600H (балл 17 200) · RTX 3050 (балл 8 900) · 16GB · SSD 512GB
Цена продавца: €520 · Рынок: €650 (медиана, n=12) · Отклонение: −20%

Цена/качество: за эти деньги (±10%) ничего мощнее нет ✓
  (или: ⚠ есть мощнее: CPU — …, GPU — …)

🔗 ссылка   [кнопка: Открыть на KP]
```
Для CHECK по «мощнее за те же деньги» — блок альтернатив (топ по CPU и топ по GPU отдельно).

---

## 5. ТЗ РЕАЛИЗАЦИИ (по файлам, максимально конкретно)

### 5.1 Бан MacBook (Фаза A)
- `collector/spam.go`: новая `IsBannedModel(title string) (bool, string)` — нормализованный
  поиск подстрок из списка банов (по умолчанию `["macbook"]`). Чистая функция, без I/O.
- `models/models.go`: статус `StatusSkippedBan = "SKIPPED_BAN"`.
- `main.go pollOnce`: сразу после `Exists`/`InsertListing`, **до** `FetchDetail`:
  `if banned, reason := collector.IsBannedModel(ad.Name); banned { SetStatus(SKIPPED_BAN); continue }`.
- `funnel/funnel.go decideL0`: дублирующая проверка по title+snippet (защита, если лот пришёл
  другими путями) → код `BAN_MAC` + счётчик.
- `config.go`: `BANNED_MODELS` (csv, по умолчанию `macbook`) — расширяемо без кода.

### 5.2 dGPU-гейт (Фаза A)
- `funnel/funnel.go`: после L3 (перед L4) — если `gpuModel == ""` или помечено «integrated»
  → тихий исход `NO_GPU` + счётчик. (CPU-ветка MANUAL остаётся как есть.)
- `specs`: флаг «integrated» уже есть (`GeminiSpecs.GPUIntegrated`). Для regex-пути:
  если regex GPU пуст и Gemini подтвердил «integrated»/ничего — считаем отсутствующим.
- `config.go`: `REQUIRE_DGPU` (по умолчанию 1) — рубильник.

### 5.3 dGPU-пул и GPU-измерение в медианах (Фаза B)
- `pricing/market.go`:
  - `Options.DGPUOnly bool` — `statsPool` дополнительно требует `l.GPUScore > 0`;
  - ключи групп для dGPU-режима: `keyK0 = cpu|ram|ssd|gpuModel`, `keyK1 = cpu|ram|gpuModel`,
    `keyK2 = cpu|gpuModel` (GPU-измерение на всех уровнях);
  - `LoadMarket` воронки вызывается с `DGPUOnly: true` (остальные потребители — cmd/analyze —
    продолжают работать с полным рынком, ничего не ломаем).
- `pricing/alternatives.go`:
  - `StrongerCPUForBudget(target, windowDays, limit, bandRatio, strongerRatio)` — кандидаты
    dGPU-пула в коридоре `[цена×(1−band), цена×(1+band)]` с `cpu_score ≥ target×(1+stronger)`;
  - `StrongerGPUForBudget(...)` — то же по `gpu_score`;
  - обе используют только лоты с `gpu_score>0`.
- Старый `StrongerForBudget` оставляем (используется теневым режимом/отчётами), но воронка
  переходит на две новые функции.

### 5.4 Интернет «модель→железо» (Фаза C)
- Новый `vision/model_specs.go`: `ModelSpecsResearch(ctx, gem, model, title) (specs.GeminiSpecs, error)`
  — использует уже готовый `GeminiClient.GenerateWithSearch`; промпт: «найди в интернете
  официальные характеристики модели ноутбука X: точный CPU и GPU»; парсинг терпимый, как в research.go;
  результат валидируется по hw.db в воронке (нет балла → не принято).
- `funnel/funnel.go`: ступень L3.4 вызывается, когда `laptopModel != ""` и не хватает CPU или GPU.

### 5.5 Перестройка L5/L6 (Фаза B)
- `funnel/funnel.go`:
  - `l5Input`: заменить одно поле `HasStronger` на `HasStrongerCPU`, `HasStrongerGPU`;
  - `decideL5` — по таблице 3.8: сначала dev-пороги, потом CPU/GPU-гейты;
  - вызов в `Run`: `strongerCPU := market.StrongerCPUForBudget(lot, 60, 1, band, stronger)`,
    `strongerGPU := market.StrongerGPUForBudget(lot, 60, 1, band, stronger)`;
  - тексты алертов (`diamondAlertText`/`checkAlertText`) — формат досье из раздела 4
    (баллы CPU/GPU, блок «мощнее за эти деньги» по двум осям).
- `config.go`: `PRICE_BAND_PCT` (10), `STRONGER_PCT` (10, диапазон 5–10), `MARKET_TOL_PCT` (5).

### 5.6 Расширение GPU-распознавания (Фаза C)
- `specs/specs.go ExtractGPU`: добавить рабочие/профессиональные GPU, которых сейчас нет:
  `RTX A####` (A2000/A3000…), `Quadro`, `T###` (T1200/T2000), `Radeon Pro`, `Intel Arc`.
  Регрессионные тесты на каждый новый паттерн (сейчас «T2000» и «RTX A3000» не распознаются — проверено тестами).

### 5.7 Тесты (к каждой фазе)
- `filters`/`collector`: MacBook-бан (в т.ч. регистр/диакритика);
- `funnel`: NO_GPU (встройка/пусто → тихо), BAN_MAC, L5-гейты (мощнее CPU → CHECK,
  мощнее GPU → CHECK, ничего → DIAMOND/GOOD), порог EXPENSIVE;
- `pricing`: StrongerCPU/GPUForBudget на синтетическом пуле, dGPU-фильтр statsPool,
  GPU-измерение в ключах групп;
- `specs`: новые GPU-паттерны.

---

## 6. ПОЭТАПНЫЙ ВЫХОД ИЗ БЕТЫ

### Фаза A — «убрать шум» (малый риск, мгновенный эффект)
MacBook-бан + dGPU-гейт.
- Результат: исчезают алерты офисных ноутбуков без видеокарты (твой пример с EliteBook) и все MacBook.
- Приёмка: `funnel-demo` на офисном лоте без dGPU → NO_GPU тихо; на MacBook → BAN_MAC тихо;
  все тесты зелёные; сутки работы — в Telegram ни одного лота без dGPU.

### Фаза B — «цена/качество» (ядро ценности)
dGPU-пул + GPU-измерение в медианах + CPU/GPU-гейты + новый вердикт/досье.
- Результат: каждый алерт — лучший по цена/качество в своём бюджете; сравнения только внутри dGPU-сегмента.
- Приёмка: синтетические тесты гейтов; `funnel-demo -random` на игровом лоте показывает
  медиану dGPU-группы и вердикт GOOD/DIAMOND/CHECK по двум осям; сутки работы — проверка адекватности.

### Фаза C — «полнота распознавания»
интернет «модель→железо» + расширение GPU-regex.
- Результат: меньше MANUAL/NO_GPU за счёт распознавания по модели; ловятся рабочие станции (Quadro/T/RTX A).
- Приёмка: `funnel-demo` на лоте, где regex/Gemini не нашли CPU, но видна модель → спеки из интернета;
  тесты новых GPU-паттернов.

После Фазы C — контрольные сутки и перевод в «рабочую версию»: метрика успеха — доля алертов,
по которым ты реально написал продавцу (цель: большинство).

---

## 7. Проверка и безопасность запуска

- Каждая фаза: `go build ./... && go vet ./... && go test ./...` зелёные.
- Исторический shadow-режим удалён: после durable queue/outbox боевой путь один, сравнение старого Gemini-пути больше не поддерживается.
- Пороги (`PRICE_BAND_PCT`, `STRONGER_PCT`, `MARKET_TOL_PCT`, `REQUIRE_DGPU`, `BANNED_MODELS`) —
  env-переменные, калибруются без кода.
- Бюджет Gemini: интернет-ступени срабатывают только при нехватке данных (как сейчас),
  рост расходов ограниченный; каждая ступень трассируется в `data/funnel_trace.log`.
