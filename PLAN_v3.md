# ТЗ v3: фильтры и выбор «алмазов» среди хлама

> Статус: ПЛАН (не реализован). Дата: 2026-08-04.
> Контекст: ТЗ v2 (PLAN.md) выполнено — бот работает. Этот документ описывает
> переход от «AI смотрит всё подряд» к детерминированной воронке фильтров.

## 1. Текущая логика (AS-IS)

### Живой бот (работает 24/7)
1. Каждые 45 с — страница 1 поиска KP (до 30 свежих лотов).
2. Дедупликация по `ad_id` о БД (`data/kp_bot.db`), спам-фильтр по заголовку/продавцу (список слов).
3. Детали `/eds/{id}` (описание, фото, продавец) только для новых.
4. Кэш цен: in-memory срез БД (EUR-лоты за 90 дней), «похожие лоты» = ≥2 общих слова в заголовке → медиана/диапазон.
5. Gemini: фаза 1 текст (+рыночная сводка), фаза 2 фото (если модель не определена). Вердикт: is_deal / need_check.
6. Telegram: 🚀 ALERT / ⚠️ NEED CHECK. CSV-экспорт батчами.

### Исследовательский контур (создан 2026-08-04)
- `cmd/research` — датасет всего рынка: **24 618 лотов** (каркас: заголовок/цена/дата готов на 100%; описания/продавцы докачиваются в фоне, ~100–150 шт/час из-за антибота KP).
- `cmd/hwdb` — эталонная база мощности из chaynikam.info: **4 239 CPU + 874 GPU** с баллами быстродействия (`data/hw.db`).
- `specs` — детерминированное извлечение CPU/RAM/SSD/GPU из текстов (regex, покрыто тестами).
- `cmd/research -enrich` — распознавание железа по всему датасету + сверка с эталоном → `research_specs` (CPU с баллом: 4 787, GPU: 1 492 — вырастет с приходом описаний).
- `cmd/analyze` — движок цена/качество: «какое железо сколько стоит» (медианы по конфигу), лучшие сделки, вердикт по лоту (`-lot <id>`) с альтернативами «то же железо дешевле» / «мощнее за те же деньги».

### Слабости текущей оценки
1. «Похожесть» по словам заголовка — медиана «по больнице» (разные конфигурации в одной куче).
2. В медиане сидят магазины (мультилоты «€150, внутри список €235–1200»), сломанные лоты, шутки (€1, €999 999).
3. Вердикт о прибыли принимает AI субъективно, а не формула.
4. Нет фильтра магазинов по описаниям (только по словам в заголовке/имени).
5. Нет ответа на главный вопрос: «это ЛУЧШИЙ вариант по цена/качество? вот альтернативы».

## 2. Целевая логика (TO-BE): воронка «алмаз среди хлама»

Каждый новый лот проходит слои ПО ПОРЯДКУ (дёшево → дорого); до Gemini доходят только прошедшие:

```
новый лот
  L0  ценовой sanity      → цена 1–5000€, не «zamena», валюта нормализована в EUR
  L1  фильтр магазинов    → метки KP (Trgovac, kpizlog) + шаблоны описаний + число объявлений продавца
  L2  фильтр хлама        → маркеры «za delove / neispravan / ne radi», condition=broken, «на запчасти»
  L3  конфигурация        → regex specs → (нет?) Gemini-текст → (нет?) Gemini-фото → (нет?) алерт «👀 посмотри сам»
  L4  цена/качество       → медиана своей конфигурации; отклонение %;
                            альтернативы: то же железо дешевле / мощнее за эти деньги
  L5  вердикт             → 💎 АЛМАЗ / ✅ норм / ⚠️ проверка / ❌ дорого — + объяснение и альтернативы
```

### Критерий «алмаза» (рабочая формула, пороги уточним на данных)
- продавец — частник (не L1);
- состояние БУ, рабочее (не L2);
- конфигурация распознана точно (CPU с баллом в эталоне);
- цена ≤ медианы своей конфигурации на 15%+;
- нет альтернативы «мощнее на 20%+ за эту же цену»;
- (опционально) цена/1000 баллов входит в топ-20% рынка для данной категории.

## 3. План работ

### Фаза 1 — данные (идёт сейчас)
- Докачка описаний/продавцов/состояний всех 24 618 лотов (`cmd/research`, инкрементально, в фоне).
- Вехи: ~1 000 описаний → достаточно для анализа маркеров; полный рынок — несколько дней.
- После каждой тысячи — `go run ./cmd/research -enrich` (обновить research_specs).

### Фаза 2 — фильтр магазинов (L1)
- На 1000+ описаниях: частотный анализ фраз, сравнение множеств «Trgovac/kpizlog=да» vs «частники».
- Артефакты: словарь маркеров описаний (шаблоны гарантий, «SVI OGLASI», прайс-листы #01-#100…), пороги.
- Метрика: на ручной разметке 50 продавцов — recall ≥95% при точности ≥90%.
- Реализация: пакет `filters` + unit-тесты на датасете.

### Фаза 3 — фильтр хлама (L2)
- Маркеры слома/запчастей + поле KP `condition` + перекрёстная проверка ценой (слом = цена вне распределения конфигурации).
- Метрика: ни один «za delove» не проходит в алерты на тестовом наборе.

### Фаза 4 — интеграция в живого бота
- Воронка L0–L2 ПЕРЕД Gemini (экономия токенов и ложных алертов).
- Кэш цен v2: вместо «общих слов» — медианы по конфигурации (cpu_model + RAM-бандл) из research_specs; источник — research.db (полный рынок), обновляется периодическим research-сканом.
- Новый формат алерта: вердикт + отклонение от медианы + 2–3 альтернативы со ссылками.

### Фаза 5 — добивка конфигураций через Gemini (L3)
- Для лотов без распознанного CPU: Gemini-текст по описанию → Gemini-фото (шильдики) → если не вышло: NEED CHECK «👀 посмотри» пользователю.
- Результат пишется обратно в research_specs (рынок учится).

### Фаза 6 — автономность
- Периодический research-скан (cron/планировщик): рынок подвижен, медианы должны стареть медленно.
- Обновление hw.db раз в месяц (`cmd/hwdb`) — рейтинги железа меняются.
- Курс RSD→EUR из открытого API вместо константы.

### Фаза 7 (опционально)
- Отчёт «рынок сегодня» (CSV/таблица): топ конфигурации, динамика цен.
- Дашборд (веб) поверх research.db.

## 4. Риски и ограничения
- **Антибот KP**: бюджет на детали ~100–150 запросов/цикл + 30-минутные паузы → полный рынок за несколько дней; фильтры строим на выборке, не дожидаясь 100%.
- **Разметка chaynikam.info может поменяться** — парсер с самопроверкой (счётчик строк, эталонные баллы) упадёт заметно, а не тихо.
- **Магазины-хамелеоны**: часть не помечена KP как Trgovac → добираем поведенческими признаками (число лотов, шаблоны), пороги консервативные, чтобы не резать частников.
- **Скам-лоты**: «алмаз» с ценой -70% от медианы может быть приманкой → в алерте помечать «проверь продавца» при отклонении >40%.

## 5. Definition of done (весь проект)
1. Бот игнорирует 100% магазинов и хлама (проверка на размеченной выборке).
2. Каждый алерт содержит: конфигурацию, отклонение от медианы, альтернативы.
3. По лоту из Telegram одной командой получается вердикт «лучший / не лучший» с цифрами.
4. Медианы считаются по конфигурации на полном рынке, обновляются автоматически.
